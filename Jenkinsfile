pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        echo 'startBuild'
      }
    }

    stage('upload') {
      steps {
        echo 'start upload'
      }
    }

    stage('deploy') {
      steps {
        echo 'start deploy'
      }
    }

  }
}